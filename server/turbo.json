{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["../dist/server/**", "../dist/schemas/**"],
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"],
			"dependsOn": ["scripts#tsc"]
		},
		"tsc": {
			"dependsOn": ["scripts#tsc"],
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"]
		},
		"test": {
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"],
			"dependsOn": ["scripts#tsc"]
		},
		"dev": {
			"cache": false,
			"persistent": true,
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"],
			"dependsOn": ["scripts#tsc"]
		}
	}
}
