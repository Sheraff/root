{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["../dist/server/**"],
			"inputs": ["../assets/src/**", "esbuild.ts", "tsconfig.app.json", "src/**"],
			"dependsOn": ["@repo/script#build"]
		},
		"tsc:tools": {
			"inputs": [
				"../types/*.d.ts",
				"../script/src/**/*.ts",
				"tsconfig.json",
				"tsconfig.tools.json"
			]
		},
		"tsc:app": {
			"inputs": [
				"src/**/*.ts",
				"src/**/*.tsx",
				"types/*.d.ts",
				"../types/*.d.ts",
				"../shared/src/**/*.ts",
				"../script/src/**/*.ts",
				"tsconfig.json",
				"tsconfig.app.json"
			]
		},
		"tsc": {
			"dependsOn": ["@repo/server#tsc:tools", "@repo/server#tsc:app"]
		},
		"test": {
			"dependsOn": ["@repo/script#build"]
		},
		"dev": {
			"cache": false,
			"persistent": true,
			"dependsOn": ["@repo/script#build"]
		}
	}
}
