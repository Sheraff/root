{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["../dist/client/**"],
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"],
			"dependsOn": ["scripts#tsc"]
		},
		"tsc": {
			"dependsOn": ["scripts#tsc"],
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"]
		},
		"test": {
			"inputs": ["../shared/src/**", "../scripts/.dist/**"],
			"dependsOn": ["scripts#tsc"]
		},
		"dev": {
			"cache": false,
			"persistent": true,
			"inputs": ["*", "**/*", "../shared/src/**", "../scripts/.dist/**", "../types/**"],
			"dependsOn": ["scripts#tsc"]
		}
	}
}
