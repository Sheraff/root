{
	"name": "scripts",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"tsc": "tsc --noEmit",
		"build": "esbuild `find src -name '*.ts'` --outdir=src",
		"clear": "rm -rf node_modules; rm -rf .turbo; rm tsconfig.lib.tsbuildinfo; find src -name '*.js' -type f -delete"
	},
	"exports": {
		"./*": "./src/*"
	}
}