{
	"name": "shared",
	"version": "1.0.0",
	"description": "Stuff that goes both in the client and server",
	"type": "module",
	"exports": {
		"./foo": "./dist/foo/index.js"
	},
	"scripts": {
		"test": "vitest",
		"dev": "tsc --watch --outDir dist",
		"build": "esbuild src/**/*.ts --format=esm --outdir=dist --outbase=src --allow-overwrite --packages=external --bundle"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {},
	"devDependencies": {
		"esbuild": "0.19.8"
	}
}