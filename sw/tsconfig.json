{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		"lib": ["WebWorker", "ESNext"],
		"rootDir": "..",
		"baseUrl": ".",
		"paths": {
			"~/*": ["./src/*"],
			"@shared/*": ["../shared/*"]
		}
	},
	"include": ["src/**/*", "types/*.d.ts", "../shared/**/*.ts", "../types/*.d.ts"],
	"exclude": ["../node_modules", "../dist", "esbuild.mjs"]
}
