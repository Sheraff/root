{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["../dist/sw/*"],
			"dependsOn": ["client#build", "shared#tsc", "scripts#tsc"]
		},
		"tsc": {
			"dependsOn": ["shared#tsc", "scripts#tsc"],
			"inputs": ["*", "**/*", "../shared/.dist/**", "../scripts/.dist/**", "../types/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true,
			"inputs": ["../shared/.dist/**", "../scripts/.dist/**"],
			"dependsOn": ["shared#tsc", "scripts#tsc"]
		}
	}
}
